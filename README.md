# PyC2
PyC2 is a simple Python-based command-and-control framework built for learning. It uses a lightweight Flask server and a small agent script that polls for tasks, runs them, and sends results back. Easy to read and extend.
